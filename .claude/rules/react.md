# React Conventions

- Do not style visuals in business components (colors, font sizes, etc) — use CSS instead.
- Do not use `&&` for conditional component rendering — use ternary or extract to a component.
- Extract child components at module level, never define them inside parent components.
